{
  "name": "premiere-agent-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Premiere Agent, an AI video agent that orchestrates research, beat analysis, and Premiere Pro editing.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sylphiette269/Premiere--agent.git"
  },
  "homepage": "https://github.com/sylphiette269/Premiere--agent",
  "bugs": {
    "url": "https://github.com/sylphiette269/Premiere--agent/issues"
  },
  "keywords": [
    "mcp",
    "agent",
    "premiere-pro",
    "video-editing",
    "ai-agent"
  ],
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && tsc -p tsconfig.json",
    "build:packages": "npm run build --workspace packages/audio-beat-mcp --workspace packages/video-research-mcp --workspace packages/premiere-mcp",
    "test": "npm run test:packages && node --test --import tsx test/**/*.test.ts",
    "test:packages": "npm run test --workspace packages/audio-beat-mcp --workspace packages/video-research-mcp --workspace packages/premiere-mcp",
    "agent": "node dist/cli/index.js",
    "agent:dev": "node --import tsx cli/index.ts",
    "scenario:product": "node --import tsx scenarios/product-video-15s.ts",
    "scenario:music": "node --import tsx scenarios/music-video-edit.ts",
    "scenario:research": "node --import tsx scenarios/research-to-edit.ts"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
